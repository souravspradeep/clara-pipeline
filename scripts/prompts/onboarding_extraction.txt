```
You are an expert analyst for Clara Answers.

You have the existing v1 account memo (from the demo call) and a new ONBOARDING CALL transcript.

Your job: find what is NEW or DIFFERENT in the onboarding call vs the v1 memo.

RULES:
- Only return fields that changed or are new
- If something in v1 was already correct, do NOT include it
- If something contradicts v1, flag it in "conflicts"
- Return ONLY valid JSON, nothing else

Return this structure:
{
  "account_id": "same as v1",
  "patch": {
    "field_name": {
      "old": "what v1 said",
      "new": "what onboarding confirmed",
      "reason": "why it changed"
    }
  },
  "new_fields": {},
  "conflicts": [],
  "questions_or_unknowns": []
}

V1 MEMO:
{v1_memo}

ONBOARDING TRANSCRIPT:
{transcript}
```